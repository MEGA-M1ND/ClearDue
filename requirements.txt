# ClearDue -- Python dependencies.
# Versions carried over from PaySentry's verified pins (same stack, same
# LangGraph/FastAPI combination already proven to work together).

langgraph==1.2.5
langgraph-checkpoint==4.1.1
langgraph-prebuilt==1.1.0
langchain==1.3.9
langchain-core==1.4.7
langchain-openai==1.3.2
openai==2.43.0

fastapi==0.115.5
uvicorn==0.32.1
pydantic==2.10.3

python-dotenv==1.0.1
requests==2.34.2

redis==8.0.1          # REDIS_URL (redis:// / rediss://) -- the common shape
upstash-redis==1.7.0  # KV_REST_API_URL/TOKEN (Upstash's REST API)

# MCP client + server, for mcp_gateway/ and razorpay_mcp/.
# Pinned below 2.0 deliberately: mcp 2.x requires starlette >=1.0, which is
# incompatible with the fastapi 0.115.5 pinned above. 1.29 works with both.
mcp==1.29.0
starlette==0.41.3
