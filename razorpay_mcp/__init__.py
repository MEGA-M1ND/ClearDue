"""A self-hosted MCP server exposing Razorpay REST operations as MCP tools.

Stands in for Razorpay's own `razorpay/mcp` Docker image -- see server.py's
module docstring for why. Run it with `python -m razorpay_mcp`.
"""
